{
  "scripts": {
    "build": "browserify -s epub book.js -o book.bin.js && browserify -s hyphenate src/hyphenate.js -o hyphenate.bin.js && browserify -s filters viewer/filters.js -o filters.bin.js",
    "test": "mocha epub/test.js viewer/test.js"
  },
  "devDependencies": {
    "browserify": "^16.2.3",
    "eslint": "^5.15.3",
    "jszip": "^3.1.4",
    "mocha": "^6.1.4",
    "xml2js": "^0.4.19",
    "xmldoc": "^1.1.0"
  }
}
